export type mailType = {
  name: string;
  email: string;
  phone: string;
  message: string;
};

export type bannerType = {
  id: string;
  title: string;
  imageString: string;
  createdAt: string;
}